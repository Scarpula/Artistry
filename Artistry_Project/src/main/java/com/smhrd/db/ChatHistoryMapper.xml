<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.smhrd.db.ChatHistoryMapper">
  	
  <insert id="saveChatHistory" parameterType="com.smhrd.model.ChatHistory">
    <selectKey keyProperty="id" resultType="int" order="BEFORE">
        SELECT chat_history_seq.NEXTVAL FROM DUAL
    </selectKey>
    INSERT INTO chat_history (id, sender_id, receiver_id, chat_history)
    VALUES (#{id}, #{senderId}, #{receiverId}, #{chatHistory})
</insert>
  
  <select id="getChatHistory" parameterType="com.smhrd.model.ChatHistory" resultType="com.smhrd.model.ChatHistory">
    SELECT *
    FROM chat_history
    WHERE (sender_id = #{senderId} AND receiver_id = #{receiverId})
        OR (sender_id = #{receiverId} AND receiver_id = #{senderId})
    ORDER BY created_at DESC
    FETCH FIRST 1 ROWS ONLY
</select>

  
  </mapper>