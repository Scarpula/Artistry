<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.smhrd.db.ChatMessageMapper">
  	
  <insert id="insertMessage" parameterType="com.smhrd.model.ChatMessage">
  	INSERT INTO chat_messages (sender_id, receiver_id, message)
  	VALUES (#{senderId},#{receiverId},#{message})
  </insert>
  
  <select id="getChatMessages" parameterType="com.smhrd.model.ChatMessage" resultType="com.smhrd.model.ChatMessage">
  	SELECT * FROM chat_messages
    WHERE (sender_id = #{senderId} AND receiver_id = #{receiverId})
       OR (sender_id = #{receiverId} AND receiver_id = #{senderId})
    ORDER BY timestamp
  </select>
  
  </mapper>